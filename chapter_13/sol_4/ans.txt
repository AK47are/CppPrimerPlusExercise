Q: 为什么有的方法重新定义了，而有些没有重新定义？
F: 派生类会继承基类的方法。

Q: 为何没有将operator=()和operator<<()声明为虚？
F: 虚函数的引用主要是为了实现多态性，使得程序可以处理不同类型的对象，而不需要知道对象的具体类型。
   operator=()中实际的赋值操作基于左侧的对象类型，而不是基类或派生类的类型。
   operator<<()为友元函数，无法声明为虚，并且左侧对象类型已经指定。
